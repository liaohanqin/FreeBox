# FreeBox 迁移计划：任务清单

## 阶段 1：后端服务改造（基于现有 FreeBox）
- [x] **任务 1.1：调查 FreeBox 启动模式**
  - 了解如何让 FreeBox 以“无头服务”模式运行。
- [x] **任务 1.2：打包 FreeBox 为后端服务**
  - 引入 `--headless` 参数，绕过 JavaFX 渲染与弹窗逻辑。
- [x] **任务 1.3：编写启动和配置指南**
  - 编写 `BACKEND_SETUP.md`。

## 阶段 2：Emacs 客户端开发（核心）
- [ ] **任务 2.1：搭建开发分支及环境 (Emacs 端)**
  - 在 `oremacs` 中建立相关包目录和空文件。
- [ ] **任务 2.2：实现 WebSocket 客户端**
  - 使用现有的 Emacs WebSocket 库（如 `websocket.el`）。
  - 实现消息编码/解码（JSON 格式，消息码驱动）。
- [ ] **任务 2.3：实现数据模型与解析**
  - 定义 `freebox-source`（源）、`freebox-vod-info`（影视信息）等数据结构。
- [ ] **任务 2.4：实现核心业务函数**
  - 封装获取源列表、搜索视频、获取分类、影视详情、播放 URL 的接口（异步或带 callback）。
- [ ] **任务 2.5：实现 UI 界面**
  - **搜索/源选择界面**（使用 `ivy`/`helm`/`completing-read`）。
  - **命令交互界面**（使用 `hydra` 或 `transient`）。
- [ ] **任务 2.6：集成 empv 播放器**
  - 获取到播放 URL 后，调用 `empv` 进行播放。
- [ ] **任务 2.7：主要命令注册与集成**
  - 导出 `M-x freebox-*` 等相关命令。

## 阶段 3：功能增强（历史与收藏等）
- [ ] **任务 3.1：历史记录功能**
- [ ] **任务 3.2：收藏管理功能**
- [ ] **任务 3.3：文档和测试**

*(注：每完成一项任务请将 `[ ]` 更新为 `[x]`)*
